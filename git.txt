	dentro do diretorio
git init
	
git config --global user.name "user"

git config --global user.email "email"

criar um arquivo .txt

git status <!-- tem arquivos sem commit -->

git add * || git add .
git add nomedoarquivo

git commit -m "meu primeiro commit"

git log


acessar github
criar repositorio
senai-versoes-colaboracoes-3
https://github.com/Mercenaryw/senai-versoes-colaboracoes3.git

envio via https
git remote add origin https://github.com/Mercenaryw/senai-versoes-colaboracoes3.git

git remote -v        check o caminho??

git config --global credential.github.com.useHttpPath true
L-->>trabalhar por repositorio 

git push -u origem master
login e senha
check no repositorio remoto

READ.md
git pull
L> trazer as alterações do repositorio remoto
<---------------------------------------------------------->
gitignore
L>tudo que estiver dentro do gitignore eke nao permite subir
*.bmp   ->ignora todos que tenham extensão .bmp

Ramificações:
L>TRUNK
	L>Branch

git checkout -b tarefa/minha-primeira-branch
(OBS analisar a ramificação de partida Master)
para voltar
git checkout master

Merge
git merge tarefa/minha-primeira-branch

TAG:(deve ser feito no branch master)
git tag -a v1.0 -m "Minha primeira tag"
git push --tag

VIN -> para sair :q
comandos terminal linux:
Ctrl+R
history

